#include <iostream>
using namespace std;

int main(){
    int i = 3;
    cout << "Bilangan kelipatan 3: ";
    do {
        if ( i % 3 == 0){
            cout << i << " ";
        }
        i++;
    } while ( i < 10);
    return 0;
}
